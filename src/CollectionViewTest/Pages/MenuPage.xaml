<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="CollectionViewTest.Pages.MenuPage">

    <Grid RowDefinitions="Auto, *, *, *" Padding="20" RowSpacing="20">
        <Label Text="Select a repeating view implementation" Grid.Row="0"/>
        <Button Text="Collection View" Clicked="OnClicked" Style="{StaticResource Button}" Grid.Row="1"/>
        <Button Text="List View" Clicked="OnClicked" Style="{StaticResource Button}" Grid.Row="2"/>
        <Button Text="Stack Layout" Clicked="OnClicked" Style="{StaticResource Button}" Grid.Row="3"/>
    </Grid>

</ContentPage>
